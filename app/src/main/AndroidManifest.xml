<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="simcpux.sourceforge.net.testaudiomv">

    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.TestSurfaceActivity"/>
        <activity android:name=".activity.TestRecordActivity"/>
        <activity android:name=".activity.TestAudioTrackActivity"/>
        <activity android:name=".activity.CameraSurfaceActivity"/>
        <activity android:name=".activity.MediaRecorderActivity"/>
        <activity android:name=".activity.DemoActivity"/>
        <activity android:name=".activity.Demo2Activity"/>
        <activity android:name=".activity.TestActivity"/>
        <activity android:name=".activity.GLSurfaceViewActivity"/>
    </application>

</manifest>